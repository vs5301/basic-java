var prompt = require('prompt-sync')()

let amount = Number(prompt("Enter amount: "))
let promocode = prompt("Enter promo code: ")

if(amount >= 149)
    console 

console.log("Amount is: "+amount+" and type is "+ typeof amount)
console.log("Promo code is: "+promocode+" and type is "+ typeof promocode)